package executor

// // Factory represents a factory to create executors
// type Factory struct {
// 	registry map[string]Executor
// }

// // NewFactory creates a new factory
// func NewFactory() *Factory {
// 	return &Factory{
// 		registry: make(map[string]Executor),
// 	}
// }

// // Register registers an executor
// func (f *Factory) Register(name string, executor Executor) {
// 	f.registry[name] = executor
// }

// // Create creates an executor
// func (f *Factory) GetExecutor(name string) Executor {
// 	return f.registry[name]
// }
